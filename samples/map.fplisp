((lambda (map1 f a) (map1 f a))
 ((lambda (u) (u u))
  (lambda (u)
    (lambda (f a)
      (if (eq a '()) '()
          (cons (f (car a))
                ((u u) f (cdr a)))))))
 ((lambda (u) (u u))
  (lambda (u)
    (lambda (x r)
      (if (eq x '()) r
          ((u u) (cdr x) (+ (car x) r))))))
 '((1 2 3 4 5) (9 5) (-3 100 5) (42)))

